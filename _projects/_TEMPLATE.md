---
# ── Project front matter ──────────────────────────────────────────────────
# Copy this file to _projects/your-slug.md and fill it in.
# `published: false` keeps this template out of the built site — delete that
# line in your copy.
published: false

# Required
title: "Project name"
description: "One or two sentences describing the project."
category: "client work"  # e.g. "client work", "open-source", "fun"
status: "active"          # e.g. "active", "deprecated" (drives the status pill style)

# Optional
logo: "/assets/images/projects/your-slug-logo.svg"  # shown in hero and card
banner: "/assets/images/projects/your-slug-bg.svg"  # hero/card background (image or gif URL)
brand_color: "#000000"     # accent color; falls back to var(--color-primary)
external_url: "https://example.com"
repo_url: "https://github.com/sinisimattia/your-repo"

# Defaults come from _config.yml — only set these to override:
featured: false   # true surfaces it in the homepage featured section
show_title: true  # false hides the title (use when the logo is the wordmark)
---

Optional long-form project write-up goes here in Markdown.
